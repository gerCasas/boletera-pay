import Component from 'inferno-component';
import './Header.css';

class Header extends Component {
  render(props, state) {

    return (
      <div>
        
      </div>
    );
  }
}

export default Header;
